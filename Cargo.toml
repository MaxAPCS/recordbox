[workspace]
members = ["backend", "frontend"]
resolver = "3"

[workspace.package]
version = "0.1.0"
license = "GPL-3.0-only"
edition = "2024"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
